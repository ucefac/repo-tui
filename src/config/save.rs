//! Configuration saving
//!
//! This module is merged with load.rs for simplicity.
//! See `load::save_config` for the implementation.

pub use crate::config::load::backup_corrupted_config;
pub use crate::config::load::save_config;
